<% provide(:title, 'All users') %>
<h1>All <%= @total %> users</h1>
<%= will_paginate %>

<ul class="users">
  <% @users.each do |user| %>
    <%= render user  if current_user != user %>
  <% end %>
</ul>

<%= will_paginate %>
<%= link_to "Create new user", new_admin_user_path %>